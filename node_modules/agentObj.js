// entrypoint.js

//defines an agent with dependencies in the folder as defined by
//clojurescript and exports the decision making AI agent

define(
    function (require) {
        require("../../out/server/goog/bootstrap/nodejs")
        require("../../out/server/proj"),
        require("../../out/server/proj/core");

        var agentPlayer = proj.core;
        module.exports = agentPlayer;
    }
);
